# pi 代码调查回归验证

固定提交：b5332540bd702f789088aa90603cc7dd5d1084ed。模型：msee-gateway/deepseek-v4.1-flash。回归工具来源：snapshot。

baseline: 1 个已执行样本中 0 通过、1 未通过、0 无法判断、0 执行错误；可判断 1/1。未执行样本不进入分母。

grounded: 1 个已执行样本中 0 通过、0 未通过、1 无法判断、0 执行错误；可判断 0/1。未执行样本不进入分母。

## 批次预算

声明上限：{"max_cost_usd":2,"max_model_calls":4}。因达到模型调用上限而停止：已用 4 次真实模型调用（上限 4 次）。已用成本约 $0.002807（估算）。整批有 2 个格子未启动（因批次预算用尽），它们从未执行，因此没有任何结论，也不计入未通过或无法判断。

所有计划样本已执行。

| 任务 | Prompt | 样本 | 结论 | 真实模型调用 | token | 成本（估算） | 成因码 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| write-tool-gate-on-recorded-regress | baseline | 1 | failed | 3 | 20700 | 0.002473 |  |
| write-tool-gate-on-recorded-regress | grounded | 1 | inconclusive | 1 | 1323 | 0.000334 | budget_exceeded |

评测要求结构化答案和准确源码引用；失败也可能是格式或引用不匹配，需要结合回放包复核。录制不足（inconclusive）不能解释为模型退化，也不能算断言失败。没有观察到失败到通过的变化时，不声称修复效果。负向控制为人工构造，不计入真实样本。成本按本地价目表（models.json）计算，是估算而不是账单。
